# Gradle / Android Studio
.gradle/
build/
local.properties
*.iml
.idea/
captures/

# Prebuilt native lib — regenerate with ./build.sh (needs the FPC toolchain)
app/src/main/jniLibs/**/*.so

# Gradle wrapper jar is regenerated by Android Studio / `gradle wrapper`
gradle/wrapper/gradle-wrapper.jar
